{
  "name": "dwatson-ecommerce",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "install": "npm install --prefix backend",
    "start": "npm run start --prefix backend",
    "dev": "npm run dev --prefix backend",
    "heroku-postbuild": "npm install --prefix backend"
  },
  "engines": {
    "node": "20.x"
  }
}

